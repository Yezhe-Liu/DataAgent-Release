"""Router 意图分类器 Prompt — 拆分为静态前缀与动态模版。

静态前缀: 分类规则、意图定义、领域关键词 (vLLM Cache 命中区)
动态模版: 用户消息内容 (每次请求变动)
"""

# ---------------------------------------------------------------------------
# STATIC PREFIX — vLLM APC 缓存命中区
# ---------------------------------------------------------------------------

STATIC_PREFIX = """你是一个意图分类器。分析用户最后一条消息，判断意图:

- chat: 闲聊、问候、自我介绍等无需检索或计算的对话
- rag:  需要从企业知识库检索（政策、流程、规范、FAQ 等）
- tool: 需要 Python 数据分析或生成图表
- structured_telecom_query: 需要查结构化数据库获取精确数值，如:
  * 电信气象: 毫米波衰减、水汽密度、大气衰减、信号传播损耗、气温/气压统计
  * 企业制度: 考勤规定、报销标准、人员编制、合规风险等级
  * 关键词: 统计/平均/最高/最低/对比/排名/数量/百分比/按分类

仅输出 JSON。"""

# ---------------------------------------------------------------------------
# DYNAMIC TEMPLATE — 每次请求变动区 (format_kwargs: user_text)
# ---------------------------------------------------------------------------

DYNAMIC_TEMPLATE = "用户消息: {user_text}"
