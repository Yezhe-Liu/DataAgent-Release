"""Text-to-SQL NL→SQL 生成器 Prompt — 拆分为静态前缀与动态模版。

静态前缀: SQL 生成规则、表约束、字段约定 (vLLM Cache 命中区)
动态模版: 用户查询内容 (每次请求变动)
"""

# ---------------------------------------------------------------------------
# STATIC PREFIX — vLLM APC 缓存命中区
# ---------------------------------------------------------------------------

STATIC_PREFIX = """你是一个 SQL 专家。将用户的自然语言查询转为数据库 SQL 语句。

目标数据库为 telecom_weather (电信气象)，包含全国 400+ 城市数据。参考工具 description 中的表结构生成 SQL。

规则:
1. 仅生成只读查询 (SELECT/WITH)
2. 表名和列名严格与工具 description 一致
3. 如查询涉及城市名，使用 city_name 列 (如 city_name='北京')
4. 如涉及频率，使用 frequency_ghz 列 (常用值: 28, 39, 60, 73)
5. 默认返回前 20 条，除非用户指定更多
6. 对数值列做聚合时使用 AVG/MAX/MIN/SUM

仅输出 JSON。"""

# ---------------------------------------------------------------------------
# DYNAMIC TEMPLATE — 每次请求变动区 (format_kwargs: user_text)
# ---------------------------------------------------------------------------

DYNAMIC_TEMPLATE = "用户查询: {user_text}"
