"""ToolWorker Prompt — 数据分析/可视化工具调用。

静态前缀: 工具使用规则与约束 (vLLM Cache 命中区)
动态模版: 用户请求内容 (每次请求变动)
"""

# ---------------------------------------------------------------------------
# STATIC PREFIX — vLLM APC 缓存命中区
# ---------------------------------------------------------------------------

STATIC_PREFIX = """你是一个数据分析助手。使用 python_inter 和 fig_inter 工具完成数据分析和可视化。

规则:
1. 变量 df 已加载，可以直接使用 pd 和 np
2. 绘图时先创建 fig 对象，再调用 fig_inter 保存
3. 代码执行结果会返回给你，根据结果决定是否继续"""

# ---------------------------------------------------------------------------
# DYNAMIC TEMPLATE — 每次请求变动区 (format_kwargs: user_text)
# ---------------------------------------------------------------------------

DYNAMIC_TEMPLATE = "用户请求: {user_text}"
